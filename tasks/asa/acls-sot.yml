---

- name: ansible facts
  ansible.builtin.debug:
    var: ansible_facts
  delegate_to: ansible_facts

- name: Debug timestamp
  debug:
    var: ansible_date_time
  delegate_to: "localhost"

# git config --global user.email cparada@redhat.com
# git config --global user.name "Carlos Parada"
# git clone https://github.com/carlosfparada/ASA-SoT.git
# git branch today
# git checkout today
# git add .
# git commit -m "commit today"
# git push -u origin today
# Username: carlosfparada
# Password: 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

- name: Create a repo branch
  ansible.builtin.expect:
    command: "ssh -o StrictHostKeyChecking=no root@{{ 172.16.1.45 }}"
    responses:
      # 'Are you sure you want to continue connecting (yes/no/[fingerprint])': "yes"
      # 'You are forced to change your password. Please input a new password.'
      's password:':
      '# ':
        - "cd /tmp/asa"
        - 'git config --global user.email cparada@redhat.com'
        - 'git config --global user.name "Carlos Parada"'
        - 'git clone https://github.com/carlosfparada/ASA-SoT.git'
        - 'cd /tmp/asa/ASA-SoT'
        - 'echo aa >> access-policies.yml'
        - "git branch {{ ansible_date_time.iso8601_basic }}"
        - "git checkout {{ ansible_date_time.iso8601_basic }}"
        - 'git add .'
        - "git commit -m 'commit {{ ansible_date_time.iso8601_basic }}'"
        - "git push -u origin  {{ ansible_date_time.iso8601_basic }}"
        - 'exit'
      'Username:':
        - 'carlosfparada'
      'Password:':
        - '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'
  no_log: false # hide passwords on logs
